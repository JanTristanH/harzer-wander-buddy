<mvc:View controllerName="hwb.frontendhwb.controller.Map"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m"
    xmlns:vbm="sap.ui.vbm">
    <Panel headerText="{i18n>helloPanelTitle}" class="sapUiResponsiveMargin" width="auto">
        <content>
            <HBox>
                <vbm:GeoMap id="map" mapConfiguration="{mapConfig>/}" initialPosition="10.033540000;53.58708" initialZoom="12">
                    <!-- map config from https://embed.plnkr.co/plunk/k5ql5sUBbmmfJ0yU -->
                    <vbm:vos>
                        <vbm:Routes>
                        </vbm:Routes>

                        <vbm:Spots items="{/PersonalizedStampboxes}">
                            <vbm:Spot position="{longitude};{latitude}" text="{number}" labelType="Default" labelText="{name}" labelPos="5" type="{= !${hasVisited} ? 'Error' : 'Success' }" />
                        </vbm:Spots>

                    </vbm:vos>
                </vbm:GeoMap>
            </HBox>
        </content>
    </Panel>
</mvc:View>
